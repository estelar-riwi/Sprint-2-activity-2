@model Sprint2Activity2.Models.Reservation

<h1>Detalles de Reserva</h1>

<dl class="row">
    <dt class="col-sm-2">Fecha</dt>
    <dd class="col-sm-10">@Model.Date.ToShortDateString()</dd>

    <dt class="col-sm-2">Hora</dt>
    <dd class="col-sm-10">@Model.Hour</dd>

    <dt class="col-sm-2">Personas</dt>
    <dd class="col-sm-10">@Model.Num_People</dd>

    <dt class="col-sm-2">Cliente</dt>
    <dd class="col-sm-10">@Model.Customer?.Name</dd>

    <dt class="col-sm-2">Notas</dt>
    <dd class="col-sm-10">@Model.Notes</dd>
</dl>

<a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-warning">Editar</a>
<a asp-action="Index" class="btn btn-secondary">Volver</a>